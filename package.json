{
  "name": "omnicode-proxy",
  "displayName": "OmniCode Proxy — Local AI Server",
  "description": "Expose internal IDE language models (Gemini, Claude, etc.) as a local OpenAI-compatible HTTP API server. Like Ollama, but powered by the models already inside your IDE.",
  "version": "1.1.0",
  "publisher": "allwin-antony",
  "icon": "assets/image.png",
  "repository": {
    "type": "git",
    "url": "https://github.com/allwin-antony/OmniCode_Proxy.git"
  },
  "license": "MIT",
  "engines": {
    "vscode": "^1.93.0"
  },
  "enabledApiProposals": [
    "languageModelProxy"
  ],
  "categories": [
    "AI",
    "Machine Learning",
    "Other"
  ],
  "keywords": [
    "ai",
    "llm",
    "openai",
    "ollama",
    "gemini",
    "language-model",
    "api-server",
    "local-server"
  ],
  "activationEvents": [
    "onStartupFinished"
  ],
  "main": "./out/extension.js",
  "contributes": {
    "commands": [
      {
        "command": "omniBridge.startServer",
        "title": "Omni Bridge: Start Server",
        "icon": "$(play)"
      },
      {
        "command": "omniBridge.stopServer",
        "title": "Omni Bridge: Stop Server",
        "icon": "$(debug-stop)"
      },
      {
        "command": "omniBridge.restartServer",
        "title": "Omni Bridge: Restart Server",
        "icon": "$(debug-restart)"
      },
      {
        "command": "omniBridge.openControlPanel",
        "title": "Omni Bridge: Open Control Panel",
        "icon": "$(settings-gear)"
      },
      {
        "command": "omniBridge.generateToken",
        "title": "Omni Bridge: Generate API Token"
      },
      {
        "command": "omniBridge.copyEndpoint",
        "title": "Omni Bridge: Copy Endpoint URL"
      }
    ],
    "configuration": {
      "title": "Omni Bridge",
      "properties": {
        "omniBridge.port": {
          "type": "number",
          "default": 11434,
          "minimum": 1024,
          "maximum": 65535,
          "description": "Port number for the local HTTP server."
        },
        "omniBridge.host": {
          "type": "string",
          "default": "127.0.0.1",
          "enum": [
            "127.0.0.1",
            "0.0.0.0"
          ],
          "enumDescriptions": [
            "Localhost only — most secure, only this machine can access the server.",
            "All interfaces — allows LAN access (use with caution)."
          ],
          "description": "Network interface to bind the server to."
        },
        "omniBridge.autoStart": {
          "type": "boolean",
          "default": false,
          "description": "Automatically start the server when the IDE opens."
        },
        "omniBridge.authEnabled": {
          "type": "boolean",
          "default": true,
          "description": "Require a Bearer API token for all requests."
        },
        "omniBridge.defaultModel": {
          "type": "string",
          "default": "",
          "description": "Default model family to use when the request doesn't specify one. Leave empty to require explicit model selection."
        },
        "omniBridge.corsOrigins": {
          "type": "string",
          "default": "*",
          "description": "Allowed CORS origins (comma-separated). Use '*' to allow all."
        },
        "omniBridge.logLevel": {
          "type": "string",
          "default": "info",
          "enum": [
            "none",
            "error",
            "info",
            "debug"
          ],
          "enumDescriptions": [
            "No logging",
            "Errors only",
            "Errors and request summaries",
            "Verbose logging including request/response bodies"
          ],
          "description": "Log verbosity level."
        },
        "omniBridge.maxConcurrentRequests": {
          "type": "number",
          "default": 5,
          "minimum": 1,
          "maximum": 20,
          "description": "Maximum number of simultaneous inference requests."
        },
        "omniBridge.requestTimeout": {
          "type": "number",
          "default": 120000,
          "minimum": 5000,
          "maximum": 600000,
          "description": "Request timeout in milliseconds."
        }
      }
    }
  },
  "scripts": {
    "vscode:prepublish": "npm run compile",
    "compile": "tsc -p ./",
    "watch": "tsc -watch -p ./",
    "lint": "eslint src --ext ts"
  },
  "devDependencies": {
    "@types/node": "^20.11.0",
    "@types/vscode": "^1.93.0",
    "@typescript-eslint/eslint-plugin": "^7.0.0",
    "@typescript-eslint/parser": "^7.0.0",
    "eslint": "^8.56.0",
    "typescript": "^5.4.0"
  },
  "dependencies": {
    "@vscode/webview-ui-toolkit": "^1.4.0"
  }
}
